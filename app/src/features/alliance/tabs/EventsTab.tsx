import { Callout } from '../../../components/ui/Callout';

export function EventsTab() {
  return (
    <Callout kind="info">
      Раздел «События альянса» пока не описан — наполним его, когда появится точное ТЗ.
    </Callout>
  );
}
