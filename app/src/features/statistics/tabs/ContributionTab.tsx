import { Callout } from '../../../components/ui/Callout';

export function ContributionTab() {
  return (
    <Callout kind="info">
      Раздел «Анализ вклада» пока не описан — наполним его, когда появится точное ТЗ.
    </Callout>
  );
}
